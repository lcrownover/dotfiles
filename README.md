# dotfiles

Each subdirectory contains a `link` bash script that's used to set itself up.

The `link-all` script should traverse to any subdirectory that contains a `link` script and run that script.
