# Runs for ALL shells, interactive or not (login shells, scripts, tmux
# run-shell, etc). Keep it small and trim.

# case "$(uname -s)" in
#     Darwin)
#         _brew_env_cache="${XDG_CACHE_HOME:-$HOME/.cache}/brew_shellenv.zsh"
#         [[ ! -f "$_brew_env_cache" ]] && /opt/homebrew/bin/brew shellenv >| "$_brew_env_cache"
#         source "$_brew_env_cache"
#         ;;
# esac

mkdir -p "$HOME/.local/bin"
export PATH="$PATH:$HOME/.local/bin"

source "$HOME/.config/zsh/env.d/tmux.sh"
